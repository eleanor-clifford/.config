# display setup

exec_always --no-startup-id $HOME/.config/i3/display-setup.sh

workspace 1  output _METACONF_REPLACE_PRI_DISP
workspace 2  output _METACONF_REPLACE_PRI_DISP
workspace 3  output _METACONF_REPLACE_PRI_DISP
workspace 4  output _METACONF_REPLACE_PRI_DISP
workspace 5  output _METACONF_REPLACE_PRI_DISP

workspace 6  output _METACONF_REPLACE_SEC_DISP
workspace 7  output _METACONF_REPLACE_SEC_DISP
workspace 8  output _METACONF_REPLACE_SEC_DISP
workspace 9  output _METACONF_REPLACE_SEC_DISP
workspace 10 output _METACONF_REPLACE_SEC_DISP

# lock screen
exec --no-startup-id $HOME/.config/i3/lock.sh -u $HOME/Pictures/Wallpapers/current.jpg

# Numlock
exec --no-startup-id numlockx on

# change mouse horizontal scroll acceleration
exec --no-startup-id $HOME/.config/scripts/setup_mouse.sh

# background
exec_always --no-startup-id "sleep 2; feh --bg-scale $HOME/.config/i3/lock-cache/resized.png"

# beep
exec --no-startup-id xset b off

# backlight
exec --no-startup-id clight

# tray icons
exec --no-startup-id nm-applet
exec --no-startup-id blueman-applet

# gaps
for_window [class="^.*"] border pixel 2
for_window [class="^Plank"] border pixel 0

# class                 border  backgr. text    indicator child_border
client.focused          #4c7899 #285577 #ffffff #2e9ef4   #285577
#client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
client.focused_inactive #333333 #222222 #888888 #292d2e   #222222
client.unfocused        #333333 #222222 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c
client.background       #ffffff

gaps inner 10
#gaps outer 5

# fullscreen if class is "Fullscreen"
for_window [class="^Fullscreen*"] fullscreen enable

# startup apps
_METACONF_REPLACE_LOAD_CACHE_FILESYSTEMS exec --no-startup-id "/usr/bin/onedrive -m --confdir $HOME/.config/onedrive --syncdir $HOME/OneDrive-Cam"
_METACONF_REPLACE_LOAD_CACHE_FILESYSTEMS exec --no-startup-id "/usr/bin/onedrive -m --confdir $HOME/.config/onedrive-personal --syncdir $HOME/OneDrive-Personal"
_METACONF_REPLACE_LOAD_CACHE_FILESYSTEMS exec --no-startup-id "/usr/bin/dropbox"

_METACONF_REPLACE_LOAD_NETWORK_FILESYSTEMS exec --no-startup-id "/usr/bin/google-drive-ocamlfuse $HOME/GoogleDrive-Work -label default"
_METACONF_REPLACE_LOAD_NETWORK_FILESYSTEMS exec --no-startup-id "/usr/bin/google-drive-ocamlfuse $HOME/GoogleDrive-Cam -label cam"
_METACONF_REPLACE_LOAD_NETWORK_FILESYSTEMS exec --no-startup-id "/usr/bin/google-drive-ocamlfuse $HOME/GoogleDrive-Personal -label personal"

_METACONF_REPLACE_LOAD_TOUCHSCREEN_TOOLS exec --no-startup-id flameshot &
exec --no-startup-id "$HOME/.i3/titlebar.rb"

# composite manager
exec_always --no-startup-id picom -b --experimental-backends

# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Allow Control+alt+x to trigger ctrl+z to fix US layout problems
bindsym $alt+Control+x --release exec xdotool key ctrl+z

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec kitty --session $HOME/.config/kitty/session

# start some common programs
bindsym $mod+Shift+s exec spotify
bindsym $mod+Shift+f exec firefox
bindsym $mod+Shift+v exec code
bindsym $mod+Shift+u exec libreoffice
bindsym $mod+Shift+k exec nautilus
bindsym $mod+Shift+c exec freecad

# kill focused window
bindsym $mod+Shift+q exec $HOME/.config/scripts/kill-not-plank.sh

# start dmenu (a program launcher)
bindsym $mod+s exec QT_STYLE_OVERRIDE=kvantum dmenu_run
bindsym $mod+a exec jgmenu_run >/dev/null 2>&1 &
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+n focus left
bindsym $mod+e focus down
bindsym $mod+i focus up
bindsym $mod+o focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+n move left
bindsym $mod+Shift+e move down
bindsym $mod+Shift+i move up
bindsym $mod+Shift+o move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+slash split h

# split in vertical orientation
bindsym $mod+b split v

# enter fullscreen mode for the focused container
bindsym $mod+t fullscreen toggle

# enter focused mode
bindsym $mod+h exec $HOME/.config/i3/toggle-focused-mode.sh

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+r layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+f layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+exclam workspace 1
bindsym $mod+sterling workspace 2
bindsym $mod+percent workspace 3
bindsym $mod+ampersand workspace 4
bindsym $mod+bracketleft workspace 5

bindsym $mod+bracketright workspace 6
bindsym $mod+at workspace 7
bindsym $mod+dollar workspace 8
bindsym $mod+asciicircum workspace 9
bindsym $mod+asterisk workspace 10


# move focused container to workspace
bindsym $mod+Shift+exclam move container to workspace 1
bindsym $mod+Shift+sterling move container to workspace 2
bindsym $mod+Shift+percent move container to workspace 3
bindsym $mod+Shift+ampersand move container to workspace 4
bindsym $mod+Shift+bracketleft move container to workspace 5

bindsym $mod+Shift+bracketright move container to workspace 6
bindsym $mod+Shift+at move container to workspace 7
bindsym $mod+Shift+dollar move container to workspace 8
bindsym $mod+Shift+asciicircum move container to workspace 9
bindsym $mod+Shift+asterisk move container to workspace 10

# Pulse Audio controls
# bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +1% #increase sound volume
# bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -1% #decrease sound volume

bindsym XF86AudioRaiseVolume exec --no-startup-id $HOME/.config/i3/raise-volume.sh
bindsym XF86AudioLowerVolume exec --no-startup-id $HOME/.config/i3/lower-volume.sh
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle # mute sound

# Media Player controls
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioPause exec --no-startup-id playerctl play-pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous

# reload the configuration file
# bindsym $mod+Shift+b reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+p exec pkill -9 polybar; exec pkill -9 picom; restart

# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+o exec i3-msg exit

# lock i3
bindsym $mod+l exec $HOME/.config/i3/lock.sh -l dimblur
bindsym $mod+End exec systemctl suspend

# open exit screen

bindsym $mod+Escape exec oblogout --class Fullscreen

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym n resize shrink width 5 px or 5 ppt
        bindsym e resize grow height 5 px or 5 ppt
        bindsym i resize shrink height 5 px or 5 ppt
        bindsym o resize grow width 5 px or 5 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 5 px or 5 ppt
        bindsym Down resize grow height 5 px or 5 ppt
        bindsym Up resize shrink height 5 px or 5 ppt
        bindsym Right resize grow width 5 px or 5 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+p mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#        status_command i3status
#}

